version: "2"

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam
    - bodyclose
    - noctx
    - gocritic
    - prealloc
    - exportloopref
    - nilerr
    - durationcheck
    - errorlint
    - exhaustive
    - goprintffuncname
    - makezero
    - predeclared
    - rowserrcheck
    - sqlclosecheck
    - tparallel
    - wastedassign

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true
    govet:
      enable:
        - shadow
        - nilness
        - unusedwrite
    gocritic:
      enabled-tags:
        - diagnostic
        - style
        - performance
        - experimental
        - opinionated
    prealloc:
      simple: true
      range-loops: true
      for-loops: true
    exhaustive:
      default-signifies-exhaustive: true

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gosec
          - bodyclose
          - noctx
      - path: fakeshell/
        linters:
          - errcheck

run:
  timeout: 5m
