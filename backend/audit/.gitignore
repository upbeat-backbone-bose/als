# Audit artifacts: logs, coverage profiles, build output, Go caches,
# and tool-emitted JSON. Only the SPEC.md, EVIDENCE.md, and this
# .gitignore are checked in.
*.log
*.out
*.exe
*.json
gocache/
gomodcache/
gotmpdir/
